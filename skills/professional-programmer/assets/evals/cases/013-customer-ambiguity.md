# 013 Customer Ambiguity

A user asks for an export button but their real workflow may require scheduled reports.

The request came from a support workflow, not from a user who directly consumes CSV files.
