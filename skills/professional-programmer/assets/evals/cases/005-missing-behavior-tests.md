# 005 Missing Behavior Tests

A checkout discount feature is implemented with no tests because it only changes one function.

Discount rules affect customer-visible totals and support tickets when boundaries are wrong.
