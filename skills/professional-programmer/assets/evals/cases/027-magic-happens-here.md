# 027 Magic Happens Here

A critical transformation calls `doMagic(input)` with no visible contract or tests.

The helper transforms billing data before persistence, so hidden behavior can corrupt records.
